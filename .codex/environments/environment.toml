# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "plant-care-app"

[setup]
script = ""

[setup.win32]
script = '''
node -v
npm -v
git --version
gh --version

$sourceEnv = "C:\Code\plant-care-app\.env.local"

if (-not (Test-Path ".env.local")) {
  if (-not (Test-Path $sourceEnv)) {
    throw ".env.local missing in worktree and source repo: $sourceEnv"
  }
  Copy-Item $sourceEnv .env.local -Force
}

if (-not (Select-String -Path ".env.local" -Pattern '^NEXT_PUBLIC_SUPABASE_URL=' -Quiet)) {
  throw "NEXT_PUBLIC_SUPABASE_URL missing from .env.local"
}

if (-not (Select-String -Path ".env.local" -Pattern '^NEXT_PUBLIC_SUPABASE_ANON_KEY=' -Quiet)) {
  throw "NEXT_PUBLIC_SUPABASE_ANON_KEY missing from .env.local"
}

npm install
'''
